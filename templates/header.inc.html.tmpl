<!DOCTYPE html>
<html>

<head>
  <link href="../css/styles.css" rel="stylesheet" type="text/css" />
</head>

<body>
  <div id="header">
    <table>
      <tr>
        <td>{{metadata.type}}</td>
        <td>Doc ID: {{metadata.id}}</td>
      </tr>
      <tr>
        <td>Config Item: {{metadata.buildParameter.configItem}}</td>
        <td>Version: {{metadata.buildParameter.version}}</td>
      </tr>
    </table>
  </div>
</body>

</html>
